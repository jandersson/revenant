"""The game connection and the session process: the socket, the
eaccess login, the XML parser and engine, the detachable session with
its script engine, and how sibling processes are spawned and attached."""
