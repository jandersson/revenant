"""Frontend logic without a toolkit: text styling and fonts, the
stream-to-dock routing, window layout, highlights, input focus, the
crash guard — shared by the PyQt6 window (gui/) and the terminal
frontend (tui.py, which lives here too)."""
